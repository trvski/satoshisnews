extends layout.pug

block content
  .post-list
    each post in posts
      .post
        .post-title #{post.position}. #{post.title}
        .post-info #{post.score} points by #{post.poster} #{post.age} | {#post.comments} comments
