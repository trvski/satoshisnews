extends layout.pug

block content
  .story.no-rank
    .story-title: a(href=story.url) #{story.title}
    .story-tools #{story.score} points by #{story.poster} #{story.age} | #{story.replies} comments
  .comment-list
    each comment in comments
      .comment
        .comment-indent(style={width: 20 * (comment.level - 1)})
        .comment-title #{comment.poster} #{comment.age} [-]
        .comment-body #{comment.body}
        .comment-tools reply
